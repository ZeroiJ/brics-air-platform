# Shared backend + frontend deps (Render installs this for both services)
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
python-dotenv>=1.0
httpx>=0.27
requests>=2.31
tenacity>=8.2
python-multipart>=0.0.9
# Frontend (Anoushka — Streamlit dashboard)
streamlit>=1.36
folium>=0.17
plotly>=5.22
# Gemini SDK (Sarthak — AI modules)
google-genai>=1.0
